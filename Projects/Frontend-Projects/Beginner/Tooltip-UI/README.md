# Tooltip UI

A pure CSS tooltip component with multiple position options (top, right, bottom, left).

## Features

- Pure CSS implementation (no JavaScript)
- Multiple positions
- Smooth animations
- Arrow indicators

## Usage

Open `index.html` in a web browser and hover over the buttons to see tooltips.

## Technologies

- HTML5
- CSS3 (CSS-only tooltips)
